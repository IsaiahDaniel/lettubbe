import React from 'react';
import { View } from 'react-native';

// This screen won't actually be shown because the tab press is intercepted
export default function PostScreen() {
  return <View />;
}